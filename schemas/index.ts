import coordinate from './coordinate'
import location from './location'
import report from './report'
import {tableSchema} from './table'

export const schemaTypes = [location, coordinate, report, tableSchema]
